spring:
  application:
    name: stdio
  datasource:
    url: jdbc:h2:file:/Users/khanhle/Documents/odyssey/the-lab/mcp-server/stdio/data/mcp-server-stdio-database;AUTO_SERVER=true
    driver-class-name: org.h2.Driver
    username: khanh
    password: 12345
  jpa:
    hibernate:
      ddl-auto: update #update the schema if necessary
  h2:
    console:
      enabled: true
  main:
    web-application-type: none #disable web environment for STDIO
    banner-mode: off

logging:
  level:
    # complete disable the logs that is going generated by MCP server -> to make sure MCP server is not writing any unnecessary information onto the standard input & output e.g. console -> because this information will be considered as important information by MCP client, and it will start consuming that as well
    root: error

server:
  port: 8081

aws:
  access-key-id: ${AWS_ACCESS_KEY_ID}
  secret-key: ${AWS_SECRET_KEY}
  region: ${AWS_REGION}

email:
  sender: ${EMAIL_SENDER}